apiVersion: v1
kind: Service
metadata:
  name: vct-api
  labels:
    app.kubernetes.io/name: vct-api
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 18080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: vct-api
